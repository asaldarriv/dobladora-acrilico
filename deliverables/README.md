# Entregables

PDF finales enviados por el aplicativo **Buzón** de EAFIT Interactiva.
Se generan con `uv run tdg build <documento> --release` y **sí se versionan**,
para tener trazabilidad exacta de lo que se entregó y en qué fecha.

Convención: `AAAA-MM-DD_<documento>_<autor>.pdf`
